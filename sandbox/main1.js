const PI = 3.14;
let radius = 3;
let area = 0;

/* Calculate the area with the value above */
area = radius * radius * PI;
console.log(area);

radius = 4;
console.log(radius);

/*recalculate the area */
area = radius * radius * PI;
console.log(area);
